{{! Sheet Header }}
<header class='sheet-header'>
  <img
    class='profile-img'
    src='{{actor.img}}'
    data-edit='img'
    data-action='onEditImage'
    title='{{actor.name}}'
  />
  <div class="pilot-handler">
  <img
    class='pilot-img'
    src='{{actor.system.curPilot.img}}'
    title='Pilot'
    height='100'
    width='100'
  />
  <button type="button" class="removePilot-button" data-action="removePilot">Remove</button>
  </div>
  <div class='header-fields'>
    <div class='document-name'>
      <input
        name='name'
        type='text'
        value='{{actor.name}}'
        placeholder='Callsign'
      />
    <div class='document-name-pilot'>
      <input
        name="system.curPilot.name"
        type='text'
        value='{{actor.system.curPilot.name}}'
        placeholder='Pilot'
      />
    </div>
    {{! The grid classes are defined in scss/global/_grid.scss. To use,
      use both the "grid" and "grid-Ncol" class where "N" can be any number
      from 1 to 12 and will create that number of columns.  }}
    <div class='resources grid grid-6col'>

      {{! "flex-group-center" is also defined in the _grid.scss file
        and it will add a small amount of padding, a border, and will
        center all of its child elements content and text. }}
      <div class='resource flex-group-center'>
        <label for='system.hitPoints.value' class='resource-label'>
          {{localize 'AIR_MERCS.Actor.base.FIELDS.hitPoints.value.label'}}
        </label>
        <div class='resource-content flexrow flex-center flex-between'>
          <input
            type='text'
            name='system.hitPoints.value'
            value='{{system.hitPoints.value}}'
            data-dtype='Number'
          />
          <span> / </span>
          <input
            type='text'
            name='system.hitPoints.max'
            disabled='true'
            value='{{system.hitPoints.max}}'
            data-dtype='Number'
          />
        </div>
      </div>

      <div class='resource flex-group-center'>
        <label for='system.curSpeed.value' class='resource-label'>
          {{localize 'AIR_MERCS.Actor.base.FIELDS.curSpeed.value.label'}}
        </label>
        <div class='resource-content flexrow flex-center flex-between'>
          <b>{{system.minSpeed.value}}</b>
          <span> / </span>
          <input
            type='text'
            name='system.curSpeed.value'
            value='{{system.curSpeed.value}}'
            data-dtype='Number'
          />
          <span> / </span>
          <b>{{system.maxSpeed.value}}</b>
        </div>
      </div>

      <div class='resource flex-group-center'>
        <label for='system.flare.value' class='resource-label'>
          {{localize 'AIR_MERCS.Actor.base.FIELDS.flare.value.label'}}
        </label>
        <div class='resource-content flexrow flex-center flex-between'>
          <input
            type='text'
            name='system.flare.value'
            value='{{system.flare.value}}'
            data-dtype='Number'
          />
          <span>/</span>
          <input
            type='text'
            name='system.flare.max'
            disabled='true'
            value='{{system.flare.max}}'
            data-dtype='Number'
          />
        </div>
      </div>

      <div class='resource flex-group-center'>
        <label for='system.chaff.value' class='resource-label'>
          {{localize 'AIR_MERCS.Actor.base.FIELDS.chaff.value.label'}}
        </label>
        <div class='resource-content flexrow flex-center flex-between'>
          <input
            type='text'
            name='system.chaff.value'
            value='{{system.chaff.value}}'
            data-dtype='Number'
          />
          <span>/</span>
          <input
            type='text'
            name='system.chaff.max'
            disabled='true'
            value='{{system.chaff.max}}'
            data-dtype='Number'
          />
        </div>
      </div>

      <div class='resource flex-group-center'>
        <label for='system.capacity.value' class='resource-label'>
          {{localize 'AIR_MERCS.Actor.base.FIELDS.capacity'}}
        </label>
        <div class='resource-content flexrow flex-center flex-between'>
          <input
            type='text'
            disabled='true'
            value='{{system.capacity.value}}'
          />
          <span>/</span>
          <input
            type='text'
            disabled='true'
            value='{{system.capacity.max}}'
          />
        </div>
      </div>

      <div class='resource flex-group-center'>
        <label for='system.fuel.value' class='resource-label'>
          {{localize 'AIR_MERCS.Actor.base.FIELDS.fuel.value.label'}}
        </label>
        <div class='resource-content flexrow flex-center flex-between'>
          <input
            type='text'
            name='system.fuel.value'
            value='{{system.fuel.value}}'
            disabled='true'
            data-dtype='Number'
          />
          <span>/</span>
          <input
            type='text'
            name='system.fuel.max'
            disabled='true'
            value='{{system.fuel.max}}'
            data-dtype='Number'
          />
        </div>
      </div>
    </div>
  </div>
</header>